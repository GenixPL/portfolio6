import 'package:flutter/material.dart';

class GenSliverAppBar extends StatelessWidget {
  const GenSliverAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return SliverAppBar(
      title: Text('a'),
    );
  }
}
